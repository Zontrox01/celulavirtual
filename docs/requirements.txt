# Célula Virtual — dependencias Python
# Ver whitepaper_celula_virtual.md, sección 7.1, para el detalle de cuándo hace falta cada una.
# Instalación del núcleo del MVP: pip install -r requirements.txt

# --- Imprescindibles desde la Fase 0 ---
numpy
pandas
biopython
python-libsbml
pytest

# --- Necesarias a partir de la Fase 5 (visualización e interfaz de depuración) ---
matplotlib
plotly
rich

# --- Interfaz gráfica de escritorio (gui/) ---
PySide6

# --- Opcional, a evaluar (alternativa a rich para notebook) ---
# ipywidgets

# --- Futura, no en el MVP (metabolismo a escala genómica, sección 9) ---
# cobra
